# Runtime dependencies for the Docker image build cache.
# Kept separate from pyproject.toml so `pip install -r requirements.txt`
# in the Dockerfile only installs what the production image needs.

# Shared OpenAI-API-compatible SDK; Groq + xAI adapters reuse it via base_url.
openai>=1.0
python-dotenv>=1
